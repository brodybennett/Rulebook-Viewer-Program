services:
  - type: web
    name: lotm-rulebook-viewer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python rulebook_viewer.py --host 0.0.0.0 --port $PORT --source "VSCODE MD Files" --content-root draft --no-browser
    autoDeploy: true
