services:
  - type: web
    name: lotm-rulebook-viewer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python "VSCODE MD Files/rulebook_viewer.py" --host 0.0.0.0 --port $PORT --source "VSCODE MD Files" --no-browser
    autoDeploy: true
