# Archive Notes

This folder stores files removed from the active authoring flow but kept for reference.

## edit-delta

`edit-delta/` contains legacy `*.edit_delta.yml` sidecar files that used to live next to markdown files.

- Not loaded by `VSCODE MD Files/rulebook_viewer.py`.
- Not loaded by `VSCODE MD Files/lotm_sanity_check.py`.
- Not used by scripts in `VSCODE MD Files/tools/`.

Keep them if you want old review metadata. Delete them if you no longer need that history.
